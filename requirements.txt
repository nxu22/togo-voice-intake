livekit-agents[deepgram,anthropic,cartesia,silero,turn-detector]~=1.6
python-dotenv~=1.0
httpx~=0.27

# zoneinfo has no IANA database on Windows; midnight resets depend on it
tzdata~=2024.1

# dev
pytest~=8.0
pytest-asyncio~=0.24
